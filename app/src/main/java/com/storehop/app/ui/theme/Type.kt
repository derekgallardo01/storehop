package com.storehop.app.ui.theme

import androidx.compose.material3.Typography

// Material 3 default typography (system Roboto on Android). Wrapped here so future
// per-style overrides land in one file. No custom font files in v1 — keeps APK small,
// build deterministic, locale handling automatic.
val StorehopTypography: Typography = Typography()
